environment  = "prod"
location     = "eastus"
project_name = "loan-underwriting"
# admin_object_id — injected from GitHub Secret TF_VAR_ADMIN_OBJECT_ID
# container_image — injected from GitHub Actions after Docker build
