-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
bandit==1.8.0
detect-secrets==1.5.0
ruff==0.9.0
# semgrep and snyk are CLI tools — installed in CI pipeline directly, not as pip packages
