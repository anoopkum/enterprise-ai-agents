# Trivy ignore list — TEMPORARY skips, tracked for a proper fix (see task #12).
#
# Both are BUILD-TIME tooling deps pulled in transitively by the pip/setuptools
# build chain, NOT imported by the app at runtime. They only appear because the
# final image currently carries build tooling. Proper fix: a multi-stage
# Dockerfile whose runtime stage ships only the app's runtime site-packages.
# Skipped for now to unblock the pipeline; revisit and remove these lines.

# jaraco.context 5.3.0 — path traversal. Fixed in 6.1.0. Build-time only.
CVE-2026-23949

# wheel 0.45.1 — privilege escalation / arbitrary code. Fixed in 0.46.2. Build-time only.
CVE-2026-24049
